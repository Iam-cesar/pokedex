import styled from 'styled-components'
import { mainContent650px } from 'components/UI/responsive'

export const MainContentContainer = styled.section`
  display: flex;
  gap: inherit;

  ${mainContent650px}
`
